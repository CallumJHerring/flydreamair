package com.mycompany.flydreamair;

/*
import javax.swing.*;
import java.awt.event.*;

public class LabelMouseListener extends MouseAdapter {
    private JTextField targetTextField;
    private JLabel jLabel7;
    
    public LabelMouseListener(JTextField textField) {
        this.targetTextField = targetTextField;
    }
    
    public void mouesClicked(MouseEvent e) {
        JLabel label = (JLabel) e.getSource();
        
        if (e.getButton() == e.BUTTON1) 
        {
            if (label == jLabel7) {
            targetTextField.setText("Sydney");
            } else {
            targetTextField.setText(label.getText());
            }
        }
       
    }
}
*/
